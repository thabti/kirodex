---
alwaysApply: true
---

# Icons

Use `@tabler/icons-react` for all icons. Never install or import `lucide-react`.

Tabler icons use the `Icon` prefix: `IconPlus`, `IconCheck`, `IconChevronDown`, `IconTrash`, etc.
